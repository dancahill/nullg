#!/usr/bin/nesla
if (string.cmp(typeof(_SERVER), 'table')!=0) {
	global I=0;
	for (I=1;I<50001;I++) {
		include("_load.nes");
		if ((I%500)==0) {
			include("test1.nes");
			main();
			print("", I, "\r\n");
			print("this loops tens of thousands of times... just fyi...\n");
			system("ps aux | grep nesla | grep -v grep");
			#io.flush();
		}
	}
}
