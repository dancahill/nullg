#!/usr/bin/nesla
if (typeof(x=file.read("test-nsp.nsp"))=='string') {
	x=string.split(x, "?>");
	x=string.join(x, "print(\"");

	x=string.split(x, "<?nsp");
	x=string.join(x, "\");");

	exec("print(\""+x+\"\");");
}
